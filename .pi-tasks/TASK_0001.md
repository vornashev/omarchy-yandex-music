---
id: TASK_0001
state: completed
phase: done
created_at: 2026-09-04T13:48:45.455Z
updated_at: 2026-09-04T14:47:27.897Z
title: Implement “Stage 3 — Catalog” from `/home/tema/Documents/obsidian/Yandex Music/Будущие фичи.md`: extend search to tracks, artists, albums, and playlists; add search suggestions and paginated results; and create a unified catalog within the “SEARCH” tab. Entity pages must open without automatic playback, preserve the current query and search results for back navigation, and receive all artist and album links, replacing the current `play_artist`/`artistResults` flow. Album pages must show cover art, title, artists, year, genre, and manually playable tracks. Artist pages must show a photo, description, popular tracks, paginated albums and singles, and similar artists.
label: Stage 3 Catalog: Unified Search and Entity Pages
---

## raw prompt

Давай теперь реализуем "Этап 3" /home/tema/Documents/obsidian/Yandex Music/Будущие фичи.md

PLANNING IS ALREADY DONE. This prompt is the OUTPUT of an interactive planning session that has now ended; you are the implementation step. Build the thing. Do not produce a plan, a design, a proposal, or a "planning-only" deliverable, do not write a document whose acceptance is that no code changed, and do not defer the work pending user confirmation — no user is available from here on.

PLANNING DECISIONS — these were settled with the user before this task was started. They are authoritative: implement them as written, and do not re-open or contradict them. They may not cover everything; decide anything they leave open as usual.

Q1: Должны ли страницы альбома и исполнителя открываться внутри вкладки «ПОИСК», заменяя её выдачу, или в области очереди вкладки «СЕЙЧАС»? Это определит навигацию и судьбу текущего play_artist/artistResults.
A1: сделать единый каталог внутри «ПОИСКА» с возвратом к сохранённой выдаче и перевести туда все ссылки на исполнителей и альбомы
Q2: Какие блоки считать обязательными для «полноценной страницы исполнителя» и страницы альбома? Документ перечисляет больше возможностей, чем чек-лист Этапа 3, а лайки и добавление в плейлист относятся к следующим этапам.
A2: альбом — метаданные и треки; исполнитель — фото, описание, популярные треки, пагинируемая дискография и похожие исполнители; лайки сущностей, концерты, внешние ссылки, трейлеры и добавление в плейлист оставить вне Этапа 3 (accepted recommendation)

## refined prompt

GOAL

Implement “Stage 3 — Catalog” from `/home/tema/Documents/obsidian/Yandex Music/Будущие фичи.md`: extend search to tracks, artists, albums, and playlists; add search suggestions and paginated results; and create a unified catalog within the “SEARCH” tab. Entity pages must open without automatic playback, preserve the current query and search results for back navigation, and receive all artist and album links, replacing the current `play_artist`/`artistResults` flow. Album pages must show cover art, title, artists, year, genre, and manually playable tracks. Artist pages must show a photo, description, popular tracks, paginated albums and singles, and similar artists.

CONSTRAINTS

- Update the existing project in place.
- Preserve every existing entry in `requirements.txt`, including `dbus-next==0.2.3` and `yandex-music @ git+https://github.com/MarshalX/yandex-music-api.git@0fa54f2d32084a9e461bce41890d1c9ab70d91aa`.
- Keep the `yandex-music` dependency pinned to commit `0fa54f2d32084a9e461bce41890d1c9ab70d91aa`; do not move it to a floating branch or newer release.
- Use the existing synchronous client architecture; do not migrate to `ClientAsync`.
- Route all new API requests through `_api_call()` so they remain serialized and retain retries after 2, 5, and 10 seconds.
- Use relevant catalog methods from the pinned SDK, including search with `all`, `track`, `artist`, `album`, and `playlist`, plus `search_suggest()`, `albums()`, `albums_with_tracks()`, `artists_brief_info()`, `artists_info()`, `artists_about()`, `artists_direct_albums()`, `artists_discography_albums()`, and `artists_similar()` as applicable.
- Load search results and artist discography page by page; do not eagerly load complete result sets or discographies.
- Request track metadata in batches and keep collection caches in memory only.
- Preserve the search field, selected search filter, current query, and existing result set while viewing an album or artist, allowing users to return to the unchanged results.
- Redirect all existing artist and album links into the unified catalog in the “SEARCH” tab; do not retain a separate artist browsing experience in the “NOW” queue area.
- Opening an artist, album, or playlist search result must not automatically begin playback.
- Track playback from album and artist pages must remain an explicit user action.
- Do not expose raw JSON errors in the UI.
- Handle `401`, `404`, `415`, `429`, empty responses, and changed or partially missing API models without crashing the player.
- Keep failures in catalog features isolated from current playback.
- Exclude entity likes, concerts, external links, trailers, and adding tracks to playlists from Stage 3.
- Do not add album-like controls or artist like/dislike controls as part of this task.
- Do not implement podcast or `podcast_episode` search in this stage; those entities are not included in `type_="all"`.
- Do not infer new route mounts, module mappings, exported signatures, or file layouts that are not already established by the project.

KNOWN-UNKNOWNS

- The desired debounce interval, minimum input length, and presentation behavior for `search_suggest()` are unspecified.
- The page size and UI trigger for loading additional search and discography results are unspecified.
- The exact grouping and ordering of results for the “All” filter are unspecified.
- The destination and behavior when opening a playlist search result are unspecified beyond the requirement not to autoplay it.
- The fallback UI when artist photos, descriptions, genres, years, or similar artists are absent is unspecified.
- Whether albums and singles should appear in separate artist-page sections or one combined paginated discography is unspecified.
- The required cache limits and invalidation rules for search results, album pages, and artist pages are unspecified.

EXTERNAL-DEPENDENCIES

- Yandex Music  current unofficial internal API behavior for search, albums, artists, playlists, suggestions, and pagination
- yandex-music Python SDK  Client API documentation for version 3.1.0b2 at commit 0fa54f2d32084a9e461bce41890d1c9ab70d91aa

## verified tooling

python -m py_compile backend/backend.py tests/test_backend_priority_one.py tests/test_backend_lyrics.py tests/test_backend_track_info.py
bash -n bootstrap.sh install.sh uninstall.sh bin/omarchy-yandex-music
git diff --check
omarchy plugin validate .
~/.local/share/omarchy-yandex-music/venv/bin/python -m unittest discover -s tests -v

## research

FILES
/home/tema/Documents/obsidian/Yandex Music/Будущие фичи.md:578  Authoritative Stage 3 catalog roadmap and API constraints
AGENTS.md  Repository-wide coding-agent instructions
.agents/  Mandatory architecture, UX, development, status, and release handoff documentation
requirements.txt  Pinned synchronous Yandex Music SDK and dbus dependencies that must remain unchanged
backend/backend.py:290  Backend state, API serialization, caches, search, entity loading, playback actions, snapshots, and IPC routing
bin/omarchy-yandex-music:12  CLI-to-IPC argument mapping for catalog commands and entity identifiers
Panel.qml:8  Unified Search-tab catalog UI, navigation state, entity pages, pagination, suggestions, links, and explicit playback controls
WidgetLogic.qml:39  Popup-level artist navigation entry point that must route into the Search catalog
tests/  Existing backend test suite and location for catalog fake-client, pagination, caching, resilience, and no-autoplay tests
manifest.json  Plugin metadata and version synchronized with backend releases
README.md:31  English user-facing feature, navigation, and search documentation
README.ru.md:31  Russian user-facing feature, navigation, and search documentation
CHANGELOG.md:7  English unreleased catalog change notes
CHANGELOG.ru.md:7  Russian unreleased catalog change notes
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/_client/search.py:25  Pinned synchronous search and search_suggest method signatures
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/_client/albums.py:24  Pinned synchronous albums and albums_with_tracks methods
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/_client/artists.py:55  Pinned synchronous artist details, tracks, discography pagination, about, and similar methods
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/_client/playlists.py:64  Pinned synchronous playlist lookup API for search-result playlists
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/search/search.py:11  Search response sections, correction metadata, and pagination model
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/search/search_result.py:25  Typed search result totals, page sizes, ordering, and entity lists
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/search/suggestions.py:10  Search suggestion response model
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/album/album.py:12  Album metadata, artwork, artists, release fields, type, and track volumes
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/artist/artist.py:24  Artist identity, image, genre, description, and popular-track fields
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/artist/brief_info.py:25  Aggregated artist metadata, popular tracks, releases, and similar artists
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/artist/artist_info.py:15  Detailed artist photo and description response model
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/artist/about_artist.py:15  Alternate artist biography and cover response model
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/artist/artist_albums.py:11  Paginated artist album/discography response model
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/artist/artist_similar.py:12  Similar-artists response model
/home/tema/.local/share/omarchy-yandex-music/venv/lib/python3.14/site-packages/yandex_music/playlist/playlist.py:30  Playlist owner, kind, cover, track count, tracks, and fetch helpers

APIS
APP_VERSION  Backend version constant kept synchronized with plugin metadata.
Player._api_call(function, *, retry_rate_limit=True, update_loading=True) -> Any  Serializes every catalog SDK request and retries HTTP 429 after 2, 5, and 10 seconds.
Player._friendly_error(exc) -> str  Converts catalog failures into bounded user-facing messages without raw API JSON.
Player._loading(function, kind) -> None  Starts an authenticated background operation while updating loading state.
Player._tracks_from_short_page(rows) -> list[Any]  Resolves short track rows through one batched Client.tracks() call while preserving order.
Player._metadata(track) -> dict[str, str]  Serializes track, artist, artwork, and album metadata; extend for unified entity links.
Player._set_queue(tracks, name, station="", batch_id="", start_index=0, remaining_rows=None, collection_key="") -> None  Starts playback only after an explicit track action.
Player.search(query) -> None  Existing track-only search entry point to extend with filters, pagination, suggestions, and catalog state.
Player.play_search(index) -> None  Existing explicit search-track playback action to adapt to typed catalog results.
Player.play_artist(artist_id) -> None  Legacy separate artist browser to replace with non-autoplay catalog navigation.
Player.play_artist_track(index) -> None  Legacy artist-track playback action to replace or adapt for explicit catalog-page playback.
Player.close_artist() -> None  Legacy artist browser cleanup to remove from the Now Playing flow.
Player.status(include_queue=False) -> dict[str, Any]  Snapshot serializer to extend with catalog summaries, entity pages, pagination, revisions, and local errors.
Player.handle(req) -> dict[str, Any]  IPC router to extend for catalog requests and entity identifiers.
search  Existing CLI/IPC command carrying a query; extend to carry the selected entity filter and page semantics.
play_search  Existing CLI/IPC command for explicit playback of a selected search track.
artist  Existing CLI/IPC artist command whose navigation must move into the Search catalog.
play_artist_track  Existing CLI/IPC explicit artist-track playback command to replace or adapt.
close_artist  Existing CLI/IPC command for the separate artist browser to retire.
Panel.action(command, argument)  Launches CLI actions, applies optimistic loading state, and records retry context.
Panel.applyStatus(text)  Applies backend snapshots while preserving stable list models and contextualizing malformed responses.
Panel.selectPage(index)  Selects Now Playing, Library, or Search and manages search-field focus.
WidgetLogic.showArtist(artistId)  Popup-level artist link entry point to redirect to the Search catalog instead of the Now Playing queue.
Client.search(text: str, nocorrect: bool=False, type_: str="all", page: int=0, playlist_in_best: bool=True, *args, **kwargs) -> Optional[Search]  Searches page-by-page with `all`, `track`, `artist`, `album`, or `playlist`.
Client.search_suggest(part: str, *args, **kwargs) -> Optional[Suggestions]  Returns query completions and an optional best result.
Client.tracks(track_ids, with_positions: bool=True, *args, **kwargs) -> list[Track]  Fetches track metadata in batches.
Client.albums(album_ids, *args, **kwargs) -> list[Album]  Fetches one or more album metadata models.
Client.albums_with_tracks(album_id, *args, **kwargs) -> Optional[Album]  Fetches an album page with track volumes.
Client.artists_brief_info(artist_id, *args, **kwargs) -> Optional[BriefInfo]  Fetches aggregated artist metadata, popular tracks, releases, and similar artists.
Client.artists_info(artist_id, *args, **kwargs) -> Optional[ArtistInfo]  Fetches detailed artist photo and description data.
Client.artists_about(artist_id, *args, **kwargs) -> Optional[ArtistAbout]  Alternate source for artist description and covers when detailed info is absent.
Client.artists_tracks(artist_id, page: int=0, page_size: int=20, *args, **kwargs) -> Optional[ArtistTracks]  Fetches a page of artist tracks.
Client.artists_direct_albums(artist_id, page: int=0, page_size: int=20, sort_by: str="year", *args, **kwargs) -> Optional[ArtistAlbums]  Fetches a page of directly credited releases.
Client.artists_discography_albums(artist_id, page: int=0, page_size: int=20, sort_by: str="year", *args, **kwargs) -> Optional[ArtistAlbums]  Fetches artist discography incrementally.
Client.artists_similar(artist_id, *args, **kwargs) -> Optional[ArtistSimilar]  Fetches similar artists.
Client.users_playlists(kind, user_id=None, *args, **kwargs) -> Playlist | list[Playlist] | None  Loads a search-result playlist by owner and kind without starting playback.
Client.playlist(playlist_uuid: str, *args, **kwargs) -> Optional[Playlist]  Loads a playlist when the search model supplies a UUID.
Playlist.fetch_tracks(*args, **kwargs) -> list[TrackShort]  Fetches playlist short-track rows through its owner and kind.
Search  Response model exposing `text`, `best`, `tracks`, `artists`, `albums`, `playlists`, `type`, `page`, `per_page`, and spelling-correction fields.
SearchResult[T]  Typed result section exposing `type`, `total`, `per_page`, `order`, and `results`.
Suggestions  Suggestion response exposing `suggestions: list[str]` and optional `best`.
Album  Catalog model exposing `id`, `title`, `artists`, `cover_uri`, `year`, `release_date`, `genre`, `type`, `track_count`, and `volumes`.
Album.get_cover_url(size: str="200x200") -> str  Builds an album artwork URL when `cover_uri` is available.
Artist  Catalog model exposing `id`, `name`, `cover`, `og_image`, `op_image`, `genres`, `popular_tracks`, and description fallbacks.
Artist.get_og_image_url(size: str="200x200") -> str  Builds an artist Open Graph image URL when available.
Artist.get_op_image_url(size: str="200x200") -> str  Builds the alternate artist image URL when no cover is available.
BriefInfo  Aggregated artist model exposing `artist`, `albums`, `last_releases`, `popular_tracks`, and `similar_artists`.
ArtistInfo  Detailed artist model exposing `artist`, `covers`, and `description`.
ArtistAbout  Alternate artist model exposing `artist`, `covers`, and `description`.
ArtistAlbums  Paginated release model exposing `albums` and `pager`.
ArtistSimilar  Similarity model exposing `artist` and `similar_artists`.
Pager  Pagination model exposing `total`, `page`, and `per_page`.
Playlist  Catalog model exposing `uid`, `kind`, `playlist_uuid`, `owner`, `title`, `track_count`, `cover`, `og_image`, and `tracks`.
Playlist.playlist_id -> str  Produces the canonical owner-and-kind playlist identifier.
Cover.get_url(index: int=0, size: str="200x200") -> str  Builds an entity artwork URL from `uri` or `items_uri`.
python -m unittest discover  Runs the backend fake-client and resilience test suite.
git diff --check  Validates changed files for whitespace errors.

CONTEXT
- `requirements.txt` pins `dbus-next==0.2.3` and the synchronous `yandex-music` SDK to commit `0fa54f2d32084a9e461bce41890d1c9ab70d91aa`; preserve both entries unchanged.
- `backend/backend.py` centralizes SDK serialization and HTTP 429 retries in `Player._api_call()` with delays `(2, 5, 10)`.
- `Player._loading()` starts authenticated work in a background thread and returns immediately, allowing the CLI request to finish while `Panel.qml` polls `details`.
- `Player._set_error()` writes into global player state, so catalog failures need catalog-local error state rather than this helper to remain isolated from playback UI.
- Lyrics and track-info loaders establish the existing isolation pattern: per-feature snapshots, worker threads, generation/client guards, contextual errors, and bounded in-memory LRU caches.
- Logout currently clears feature caches and increments their generations; every new catalog cache and in-flight generation must receive equivalent cleanup.
- Compact `status` is polled by `WidgetLogic.qml`, while the open panel polls heavier `details`; large catalog lists should follow the existing revision-backed list pattern instead of expanding frequent compact polling.
- `Panel.applyStatus()` preserves queue and library arrays when their revisions are unchanged, but `searchResults` is currently rebound on every poll; paginated catalog lists need independent revisions/local display models to preserve scroll position and unchanged back-navigation state.
- Search is currently track-only, stores raw track models in `Player.search_results`, caps results at 30, and starts playback through `play_search`.
- The legacy artist browser eagerly requests up to 100 tracks, stores them in `artist_results`, and renders them inside the “СЕЙЧАС” queue area; this entire state/UI path is the flow being replaced.
- Existing artist links in `Panel.qml` call `artist` from both the hero metadata and queue delegates, while `WidgetLogic.showArtist()` explicitly selects page `0`; all must instead select page `2` and open a catalog entity without playback.
- Album names are currently plain text in the hero, queue metadata, and track-info card, so album navigation requires adding explicit catalog links at those established metadata surfaces.
- `Player._set_queue()` immediately replaces the queue and invokes `_play_current()`; entity-opening commands must never call it, while explicit track-row actions may use it.
- Search page `2` disables the outer `panelScroll` and gives scrolling to a fixed-height internal viewport; entity pages and paginated sections must retain internal virtualized `ListView`/`Flickable` scrolling.
- The search `TextField` is a persistent QML object and page selection already manages its focus; catalog entity navigation should swap only the Search-tab body so the entered text remains intact.
- `Panel.action()` and `WidgetLogic.action()` accept only one optional positional argument, and `bin/omarchy-yandex-music` explicitly maps command arguments into IPC fields; filters, pages, entity IDs, and playlist owner/kind data require coordinated mappings across all three layers.
- The CLI socket timeout is eight seconds, so catalog commands should preserve the existing asynchronous-start-and-poll architecture rather than block the IPC response during serialized API calls and backoff.
- `_tracks_from_short_page()` is the established batch metadata resolver and preserves source order; catalog track pages should reuse or generalize this path instead of calling per-row fetch helpers.
- `_metadata()` already normalizes track artists and artwork but exposes album title without album ID; unified album links require extending normalized track snapshots defensively.
- Current `_friendly_error()` can return a truncated raw exception string; catalog snapshots need sanitized status-specific messages for `401`, `404`, `415`, `429`, malformed models, and empty responses.
- SDK response shapes and missing-field behavior are not verified by the provided external context; catalog normalization must use guarded attribute access and tolerate `None`, absent sections, and partial models.
- `Panel.qml` uses a single `actionProcess`, optimistic loading kinds, and retry bookkeeping; suggestion requests need a separate cancellable/debounced process or generation mechanism so they do not block normal catalog actions or apply stale responses.
- `APP_VERSION` in `backend/backend.py` and `manifest.json.version` are both `0.8.0` and are required by project checks to remain synchronized after version changes.
- Substantial implementation requires updating the repository handoff status plus parallel English/Russian README and changelog documentation.

VERIFIED-TOOLING
  python -m py_compile backend/backend.py tests/test_backend_priority_one.py tests/test_backend_lyrics.py tests/test_backend_track_info.py
  bash -n bootstrap.sh install.sh uninstall.sh bin/omarchy-yandex-music
  git diff --check
  omarchy plugin validate .
  ~/.local/share/omarchy-yandex-music/venv/bin/python -m unittest discover -s tests -v

## grill Q&A

Q1: When a playlist search result is opened, should it display a playlist detail page in the Search tab with explicitly playable tracks, or navigate to an existing playlist view?
A1: display a playlist detail page in the Search tab with explicitly playable tracks
Q2: For the “All” search filter, should results appear in separate sections by entity type or as one interleaved list following the API’s ordering?
A2: show separate sections for tracks, artists, albums, and playlists
Q3: On artist pages, should albums and singles be shown as separate independently paginated sections, or as one combined paginated discography?
A3: show albums and singles as separate independently paginated sections
Q4: How should search suggestions behave: what debounce interval and minimum query length should trigger them, and should they appear as a dropdown that submits the selected suggestion?
A4: debounce for 300 ms, trigger at 2 characters, and show a dropdown whose selected suggestion submits a search. (auto)

## spec

GOAL

Implement “Stage 3 — Catalog” as a unified experience in the “SEARCH” tab. Support suggestions and paginated search across tracks, artists, albums, and playlists; sectioned “All” results; non-autoplay album, artist, and playlist detail pages; explicit track playback; and back navigation that preserves the active search. Redirect all existing artist and album links away from the legacy `play_artist`/`artistResults` flow into this catalog.

CONSTRAINTS

- Update the existing project in place.
- Preserve every existing entry in `requirements.txt`, including `dbus-next==0.2.3` and `yandex-music @ git+https://github.com/MarshalX/yandex-music-api.git@0fa54f2d32084a9e461bce41890d1c9ab70d91aa`.
- Keep `yandex-music` pinned to commit `0fa54f2d32084a9e461bce41890d1c9ab70d91aa`; do not move it to another commit, branch, or release.
- Keep the synchronous client architecture; do not migrate to `ClientAsync`.
- Route every new SDK request through `Player._api_call()` so requests remain serialized and retain retries after 2, 5, and 10 seconds.
- Do not bypass `_api_call()` serialization to improve suggestion responsiveness. Use generation/client guards to discard stale suggestion and catalog responses, recognizing that an in-flight synchronous request may delay subsequent API calls.
- Do not infer new route mounts, module mappings, exported signatures, or production file layouts that are not already established by the project.
- Support search types `all`, `track`, `artist`, `album`, and `playlist` through the pinned SDK.
- Use relevant pinned-SDK methods as applicable, including `search()`, `search_suggest()`, `albums()`, `albums_with_tracks()`, `artists_brief_info()`, `artists_info()`, `artists_about()`, `artists_tracks()`, `artists_direct_albums()`, `artists_discography_albums()`, and `artists_similar()`.
- Render `all` results as four separate sections: Tracks, Artists, Albums, and Playlists.
- Load search results page by page. An explicit load-more action must append the next available page without replacing existing rows or introducing duplicates.
- Trigger `search_suggest()` after 300 ms of inactivity when the trimmed query contains at least two characters. Show results in a dropdown; selecting a suggestion submits a normal search. Hide or clear suggestions below the minimum length, and prevent older responses from replacing suggestions for newer input.
- Preserve the search field text, selected filter, current query, loaded pages, and existing result models while an entity page is open. Back navigation must restore that unchanged search state.
- Opening an artist, album, or playlist result must navigate to its detail page in the Search tab without calling `_set_queue()` or otherwise changing playback.
- Display playlist details in the Search tab with explicitly playable tracks. Load the playlist using identifiers available in the search model, using `playlist()` when a UUID is available or `users_playlists()` when owner and kind are available.
- Album pages must show available cover art, title, linked artists, year or release-date fallback, genre, and tracks. Missing optional metadata must not prevent the page from rendering. Playback requires an explicit track action.
- Artist pages must show available photo, description, popular tracks, similar artists, and separate Albums and Singles sections. Albums and Singles must be independently paginated and have separate load-more actions.
- Classify artist releases defensively using available SDK model metadata; missing or changed fields must not crash or discard the rest of the artist page.
- Request track metadata in batches through `_tracks_from_short_page()` or an equivalent batch path using `Client.tracks()`; do not fetch metadata once per row.
- Keep catalog collections and entity caches in memory only. Clear them on logout and use generation/client guards so work from an earlier client session cannot update the current session.
- Extend normalized track metadata defensively with available album identifiers needed by catalog links while tolerating missing and partially populated fields.
- Redirect all established artist and album links, including hero metadata, queue delegates, track-information surfaces, panel links, and `WidgetLogic.showArtist()`, to the unified catalog and the existing Search page index `2`.
- Remove the legacy artist browsing UI from the Now Playing queue area. Existing command entry points may remain only where needed to redirect callers to non-autoplay catalog navigation.
- Search-result tracks and tracks on album, artist, or playlist pages may start playback only through an explicit user action. Catalog loading or playback failures must not replace or corrupt the current queue.
- Keep catalog errors local to catalog state rather than placing them in the player’s global error state.
- Handle `401`, `404`, `415`, `429`, empty responses, malformed models, and partially missing fields with contextual user-facing catalog states. Do not expose raw JSON or crash the player.
- Use virtualized internal scrolling for search and entity result lists, consistent with the Search tab’s disabled outer `panelScroll`.
- Do not implement entity likes, album-like controls, artist like/dislike controls, concerts, external links, trailers, adding tracks to playlists, podcasts, or `podcast_episode` search.
- Add fake-client backend coverage for typed and all-filter search, suggestions and stale generations, pagination, batched metadata, in-memory cache cleanup, partial models, handled API failures, preserved catalog state, playlist loading, and non-autoplay entity navigation.
- Add automated QML interaction coverage for suggestion timing and selection, sectioned results, load-more behavior, entity navigation and back-state restoration, and explicit-playback-only controls.

ACCEPTANCE

- All, Tracks, Artists, Albums, and Playlists searches use the corresponding SDK search type, and All displays four distinct entity sections.
- Suggestions are requested only after two trimmed characters and 300 ms of inactivity. They appear in a dropdown, selecting one submits a search, and a stale response cannot overwrite newer input.
- Load more appends the next search page to the applicable displayed section or sections without removing existing rows, adding duplicates, or resetting the preserved search state.
- Opening an artist, album, or playlist leaves the current track and queue unchanged and displays the entity inside the Search tab.
- Returning from an entity page restores the same field text, filter, query, loaded pages, and result models.
- Album pages render all available required metadata, tolerate absent optional fields, link their artists into the catalog, and play tracks only after an explicit track action.
- Artist pages render available identity, biography, popular tracks, and similar artists. Albums and Singles are separate and can load additional pages independently.
- Playlist pages resolve the identifiers provided by search results and expose tracks without autoplay.
- Every established artist and album link navigates to the unified Search catalog rather than the legacy Now Playing artist browser.
- The legacy `artistResults` browsing UI no longer replaces or occupies the Now Playing queue.
- Relevant album and artist loading uses the listed pinned-SDK methods through `_api_call()`, and track metadata is requested in batches rather than once per row.
- Catalog and suggestion responses from stale generations or a logged-out client are discarded; logout clears catalog caches.
- Handled HTTP errors, empty responses, malformed models, and missing fields produce safe catalog-local states without exposing raw JSON or interrupting current playback.
- Automated backend and QML tests exercise suggestions, sectioned results, pagination, entity navigation, restored search state, and the requirement that playback occurs only after an explicit track action.
- Python compilation, shell syntax checks, plugin validation, the complete unit-test suite, QML interaction tests, and whitespace validation pass.

VERIFY:
```sh
python -m compileall -q backend tests
bash -n bootstrap.sh install.sh uninstall.sh bin/omarchy-yandex-music
omarchy plugin validate .
~/.local/share/omarchy-yandex-music/venv/bin/python -m unittest discover -s tests -v
QMLTESTRUNNER="$(command -v qmltestrunner || command -v qmltestrunner6 || { test -x /usr/lib/qt6/bin/qmltestrunner && printf '%s\n' /usr/lib/qt6/bin/qmltestrunner; })"
test -n "$QMLTESTRUNNER"
QML_TEST_DIR="$(find tests -type f -name 'tst_*.qml' -printf '%h\n' | sort -u | head -n 1)"
test -n "$QML_TEST_DIR"
QT_QPA_PLATFORM=offscreen "$QMLTESTRUNNER" -input "$QML_TEST_DIR" -import .
git diff --check
```

## phase timings

refine               31.4s
research            369.1s
  enrichment              4.0s
  worker:files wait      382ms
  worker:files work     122.9s
  worker:apis wait       543ms
  worker:apis work       98.8s
  worker:apis discarded (1 restart)   36.4s
  worker:context wait    391ms
  worker:context work    62.8s
  worker:tooling wait    396ms
  worker:tooling work    19.2s
  workers               345.9s
  verify-tooling         23.2s
grill               484.9s
  gen                     5.2s
  auto-answer             4.1s
  gen                     6.8s
  auto-answer             4.8s
  gen                     4.6s
  auto-answer             4.7s
  gen                     7.3s
  gen                     6.6s
  auto-answer             5.5s
  gen                     4.0s
  gen                     4.1s
compose              40.7s
critique            157.7s
  triage                 44.8s
  rewrite               112.9s
total              1083.8s

## handoff

handoff_at: 2026-09-04T14:06:52.266Z

## gates

- 2026-09-04T14:43:13.245Z verify: FAIL — verify child mutated repo state and its verdict was discarded (state restored: removed child-created file .pi/fusion/01a06cad-cfa1-7199-a8ed-437c673b9405-1957500/validate-fb3fe36c90d36d07dde382ab158b1bea/candidate-1.attempt-1.events.jsonl; removed child-created file .pi/fusion/0)
- 2026-09-04T14:43:41.342Z resolution: recommended ACCEPT
- 2026-09-04T14:45:50.594Z resolution: user ACCEPTED the work despite verify FAIL
- 2026-09-04T14:45:50.621Z commit: task snapshot committed
- 2026-09-04T14:47:27.896Z enforce(flag): guideline violation: нарушены запрет на commit без явного подтверждения владельца, инвариант explicit track selection и требование не включать тяжёлые списки каталога в частый status polling

